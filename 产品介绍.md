# CDMO Quotation Management Platform — 产品介绍

**版本**: v1.0.0  
**更新日期**: 2026-06-09  
**产品定位**: CDMO（合同研发生产组织）报价管理平台  

---

## 1. 产品基础定位

### 1.1 产品背景
CDMO 行业报价流程复杂：商品种类多（原料药、制剂、分析、注册），计价单位不统一（按项目/按批/按项），报价单编制容易出错。本平台为 CDMO 销售团队提供标准化的商品管理和报价编制工具。

### 1.2 目标用户
- **销售员（Salesperson）**：管理商品库、创建报价单、提交审核
- **审核人（Reviewer）**：查看报价单（审核功能 v2 预留）

### 1.3 核心价值
- 集中管理 CDMO 商品目录，统一计价单位
- 报价编制时自动带入销售指导价，减少人工录入错误
- 计价单位智能逻辑：项目型商品数量锁定为 1，批/项型商品可填数量
- 自动计算行总价和报价汇总，按报价金额汇总（非指导价）

---

## 2. 完整功能清单

### 2.1 用户认证
- 用户名 + 密码登录
- 角色区分：销售员（salesperson）/ 审核人（reviewer）
- 登录后按角色路由到对应功能
- Demo 账号：sales / reviewer，密码 123456

### 2.2 商品管理模块
- **商品列表**：展示全部商品（Product ID、商品类型、指导价、计价单位、状态、创建时间）
- **新建商品**：录入商品类型、销售指导价、计价单位（project/batch/study）
- **商品编号**：自动生成（P + 日期 + 序号）
- **状态管理**：Active（在售）/ Inactive（停售）

### 2.3 报价管理模块
- **报价单列表**：展示全部报价单（报价单号、客户、预算、报价总额、状态、编制人、日期）
- **创建报价单**（单页两步）：
  - Step 1 基础信息：客户名称、预算金额、需求详情
  - Step 2 明细编制：选择商品 → 自动带入指导价 → 人工录入报价单价和数量 → 自动计算行总价
- **报价单详情**：点击列表行查看完整报价单（基础信息 + 明细行 + 汇总）
- **提交审核**：报价单从 Draft → Submitted 状态流转
- **存草稿**：支持保存为 Draft 草稿状态

### 2.4 业务逻辑
- 商品计价单位 = project → 数量锁定为 1（不可编辑）
- 商品计价单位 = batch/study → 数量可编辑（≥1 整数）
- 行总价 = 报价单价 × 数量（自动计算）
- 报价汇总 = 所有行总价之和（按报价金额汇总，非指导价）

---

## 3. AI 技术架构

```
┌─────────────────────────────────────────┐
│              Streamlit Frontend          │
│  Home.py (Login)                        │
│  pages/1_Product_Management.py          │
│  pages/2_Quotation_Management.py        │
├─────────────────────────────────────────┤
│           Business Logic Layer           │
│  src/auth.py (Authentication)           │
│  src/components.py (UI Components)      │
├─────────────────────────────────────────┤
│           Data Access Layer              │
│  src/database.py (SQLite CRUD)          │
├─────────────────────────────────────────┤
│              SQLite Database             │
│  users | products | quotations | items  │
└─────────────────────────────────────────┘
```

---

## 4. 大模型选型说明

本平台 v1.0.0 为纯前端 + 本地数据库应用，不涉及大模型调用。后续版本可考虑集成 LLM 用于：
- 报价单智能摘要生成
- 客户需求解析 → 自动推荐商品组合
- 报价合理性检查

---

## 5. Skill 工具能力

本平台为独立 Streamlit 应用，不包含外部 Skill 工具。内部模块：
- `src/database.py`：数据库连接、表创建、种子数据、CRUD 操作
- `src/auth.py`：用户认证、会话管理
- `src/components.py`：UI 复用组件（CSS、侧边栏、状态徽章、价格格式化）

---

## 6. 调用方式说明

- **启动方式**：`streamlit run Home.py` 或 `python3 -m streamlit run Home.py`
- **数据库**：SQLite 本地文件（`data/app.db`），首次启动自动建表 + 种子数据
- **认证**：SHA-256 密码哈希，Streamlit session_state 管理会话
- **无需外部 API 调用**

---

## 7. 完整工作流

### 7.1 用户登录
```
打开 Home.py → 输入用户名/密码 → 验证 → 进入侧边栏导航
```

### 7.2 商品管理
```
侧边栏点击 Product Management → 查看商品列表 → Tab 切换到 New Product → 填写类型/价格/单位 → 创建
```

### 7.3 创建报价单
```
侧边栏点击 Quotation Management → 点击 Create Quotation →
填写客户/预算/需求 → 选择商品添加 → 修改报价/数量 → 自动计算 →
点击 Submit for Review 或 Save as Draft
```

### 7.4 查看报价单
```
报价单列表 → 点击某行 View 按钮 → 查看详情（基础信息 + 明细 + 汇总）→ Back to List
```

---

## 8. 提示词工程

本平台不涉及 LLM 提示词。前端界面文案全部英文，适配国际化 CDMO 场景。

---

## 9. 异常与边界处理

| 场景 | 处理方式 |
|------|---------|
| 数据库不存在 | 首次启动自动创建表和种子数据 |
| 未登录访问页面 | 自动跳转到登录页 |
| 商品类型为空 | 前端校验，提示必填 |
| 报价金额 ≤ 0 | 前端校验，拒绝提交 |
| 无限行提交 | 前端校验，至少 1 行 |
| 重复添加同一商品 | 前端拦截，提示已存在 |
| 报价单不存在 | 返回列表页并提示错误 |

---

## 10. 迭代状态

- **当前版本**：v1.0.0
- **已知缺陷**：无
- **待优化方向**：
  - 审核人审核工作流（通过/驳回 + 意见）
  - 报价单 PDF 导出
  - 报价历史版本对比
  - 多币种支持
  - 邮件通知
