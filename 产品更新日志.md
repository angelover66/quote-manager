# Quotation Platform — 产品更新日志

---

## v1.0.1 — 2026-06-09

### 迭代类型：优化

### 本次改动
- 移除登录系统，直接访问平台
- 单页应用架构：侧边栏导航 + 主内容区
- 品牌名改为 Quotation Platform
- Product Type → Product Name
- Line Items → Products Detail
- 报价单列表 HTML 表格，黑线边框，行内 View | Edit 链接
- Edit 功能：预填数据编辑报价单，支持修改并保存
- Create Quotation 页面底部按钮始终可见，点击校验
- ← Back 返回按钮
- Add → Add Product，下拉框默认空 + 灰色提示文案
- 导航条改名：Product Management / Quotation Management
- 修复 st.session_state.items 与 dict 方法冲突
- 修复按钮参数 width vs use_container_width
- 白底黑字全局主题 (.streamlit/config.toml)
- 部署到 GitHub (angelover66/quote-manager) + Streamlit Cloud

### 当前状态
- 本地运行：`python3 -m streamlit run Home.py`
- 访问：http://localhost:8501
- Streamlit Cloud：GitHub 自动部署

---

## v1.0.0 — 2026-06-09

### 迭代基础信息
- **版本号**: v1.0.0
- **迭代类型**: 新增（首次发布）
- **更新人**: Lulu

### 本次改动核心功能
1. **用户认证系统**：多用户登录，角色区分（销售员/审核人），密码 SHA-256 哈希
2. **商品管理模块**：10 个 CDMO 商品预置，支持新建商品，自动生成商品编号
3. **报价管理模块**：报价单列表 + 创建（单页两步）+ 详情查看
4. **计价单位智能逻辑**：project 型商品数量锁定为 1，batch/study 型可编辑
5. **自动计算**：行总价 = 报价单价 × 数量，报价汇总 = 所有行总价之和

### 本次使用 AI 技术
- **前端框架**：Streamlit Multi-Page + Session State 状态管理
- **数据库**：SQLite（4 表：users / products / quotations / quotation_items）
- **设计系统**：Clean Light + Indigo（#6366f1），自建 CSS 组件库

### 开发落地难点
1. **计价单位逻辑设计**：如何让"项目型商品数量自动锁定"和"批/项型商品手动输入"在一个表格中自然呈现？最终采用 Python 3.9 兼容方案，通过 `disabled` 参数区分两种输入框。
2. **Streamlit 三视图状态管理**：列表/创建/详情三个视图如何在一个页面内切换？使用 `st.session_state.view` 驱动条件渲染，避免页面跳转。
3. **Python 3.9 兼容性**：macOS 默认 Python 3.9 不支持 `dict | None` 和 `list[dict]` 语法，需使用 `Optional[dict]` 和 `list`。

### 解决方案与取舍
- **放弃方案**：不做前后端分离（React + FastAPI），选择 Streamlit 全栈，换取开发速度和 Demo 展示便利性
- **放弃方案**：不做审核工作流（v2 预留），v1 聚焦核心编制流程
- **选择方案**：SQLite 而非 PostgreSQL，零配置本地运行，面试 Demo 开箱即用
- **选择方案**：10 个 CDMO 商品（8 核心 + 2 扩展），覆盖原料药/制剂/分析/注册四大业务线

### 产品复盘思考
- CDMO 行业报价场景比预期复杂：不同商品天然有不同的计价方式（项目/批/项），设计数据模型时必须把"计价单位"作为一等字段
- Streamlit 做管理平台可行但有限制：页面 reload 感、不支持客户端路由、复杂交互需要 session_state 桥接
- 下一步：审核工作流是 v2 的核心增量，需要增加报价单状态机（Draft → Submitted → Approved/Rejected）
